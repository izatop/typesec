{
  "name": "typesec",
  "scripts": {
    "clean": "rimraf packages/*/{dist,tsconfig.tsbuildinfo}",
    "build": "tsc -b packages",
    "build:watch": "tsc -b packages -w"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "3.5.3",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=23.9"
  },
  "private": true,
  "dependencies": {
    "@types/bun": "^1.2.8"
  },
  "resolutions": {
    "bun-types": "canary"
  }
}
